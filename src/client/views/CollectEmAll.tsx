import * as React from 'react';

const CollectEmAll: React.FC<CollectEmAllProps> = (props) => {
    
    return (
        <main className="container">
            
        </main>
    );
};

interface CollectEmAllProps {}

export default CollectEmAll;