import * as React from 'react';

const Home: React.FC<HomeProps> = (props) => {
    
    
    return (
        <main className="container">
            
        </main>
    );
};

interface HomeProps {}

export default Home;